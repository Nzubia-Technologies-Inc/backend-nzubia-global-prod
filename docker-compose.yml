version: '3.8'

services:
  # MySQL is configured remotely in .env (104.238.221.158)
  # Keeping this here for reference if local dev is needed later
  # nzubia_db:
  #   image: mysql:8.0
  #   container_name: nzubia_db
  #   restart: always
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root
  #     MYSQL_DATABASE: nzubia_global
  #   ports:
  #     - '3307:3306'
  #   volumes:
  #     - db_data:/var/lib/mysql

  nzubia_redis:
    image: redis:alpine
    container_name: nzubia_redis
    ports:
      - '6379:6379'

volumes:
  db_data:
